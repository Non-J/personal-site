---
export interface Props {
	className?: string;
}
const { className = '' } = Astro.props;
---

<header class={'flex flex-wrap justify-evenly md:justify-normal lg:flex-row gap-2 md:gap-4' + (className ? ` ${className}` : '')}>
	<slot />
</header>
